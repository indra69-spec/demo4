sudo apt update
sudo apt install openjdk-21-jdk maven git docker.io -y
java -version
mvn -version
docker --version

mvn archetype:generate -DgroupId=org.example -DartifactId=Demo2 \
  -DarchetypeArtifactId=maven-archetype-quickstart \
  -DarchetypeVersion=1.4 -DinteractiveMode=false

cd Demo2

find . -name "*.java"
This should show src/main/java/org/example/App.java. If it shows a different class name, note it — you'll swap it into pom.xml 

mvn clean package
ls target/
java -jar target/demo2.jar
This must print output with no errors before moving forward. If App.java prints Hello World!, you'll see that.

after docker file :

git init
git add .
git commit -m "Initial commit - Demo2 project"
git branch -M main
git remote add origin https://github.com/indrajitgupta1620-hub/Demo2.git
git push -u origin main

jenkins installation : 

sudo apt install fontconfig openjdk-21-jre -y

curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key | sudo tee \
  /usr/share/keyrings/jenkins-keyring.asc > /dev/null

echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null

sudo apt update
sudo apt install jenkins -y
sudo systemctl enable jenkins
sudo systemctl start jenkins

sudo cat /var/lib/jenkins/secrets/initialAdminPassword

Let Jenkins use Docker
bashsudo usermod -aG docker jenkins
sudo systemctl restart jenkins

minikube installation : 

curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
sudo install minikube-linux-amd64 /usr/local/bin/minikube

minikube start --driver=docker

let jenkins use kubectl : 
sudo mkdir -p /var/lib/jenkins/.kube
sudo cp ~/.kube/config /var/lib/jenkins/.kube/config
sudo chown -R jenkins:jenkins /var/lib/jenkins/.kube

11. Add Docker Hub Credentials in Jenkins UI
Manage Jenkins → Credentials → Global → Add Credentials:

Kind: Username with password
Username: indrajit
Password: your Docker Hub token
ID: dockerhub-creds

after running jenkins : 
docker images | grep demo2
kubectl get pods
kubectl get deployments
kubectl get svc demo2-service
minikube service demo2-service --url
